{
  "name": "auth-system-root",
  "version": "1.0.0",
  "scripts": {
    "postinstall": "npm install --prefix backend --legacy-peer-deps && npm install --prefix frontend --legacy-peer-deps",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "@renderinc/sdk": "^0.5.0",
    "stripe": "^20.4.1"
  }
}
